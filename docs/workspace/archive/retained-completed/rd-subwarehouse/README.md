# 研发小仓同平台隔离

关联需求: `docs/requirements/archive/retained-completed/req-20260326-0048-rd-subwarehouse.md`
关联任务: `docs/tasks/archive/retained-completed/task-20260326-0415-rd-subwarehouse-phase1-operating-foundation.md`
阶段: 已归档完成
创建: 2026-03-25
最后更新: 2026-03-28

## 当前状况

“主仓 + 研发小仓同平台隔离协同”已完成首个 operating foundation 切片，并将本工作流归档为基线说明。已知用户诉求是：研发有自己的小仓；主仓领出后进入小仓时，系统应能承接对应业务；小仓管理员只能看到并操作自己小仓相关的信息，不应看到主仓的无关内容。
用户已明确：研发小仓按真实独立小仓处理，当前正式建模采用“受限子仓模型”；首个切片已落地研发独立工作台、固定仓别隔离、项目领用、本仓报废、研发侧库存/流水/自动入库结果与分类报表入口。后续主仓到 RD 自动交接、研发采购需求与主仓验收联动、研发物料独立状态链，以及小仓盘点/库存调整，应在新的 requirement / task 中继续推进，而不是复用本归档工作流。

## 表格化决策摘录

### 角色 / 界面 / 权限矩阵

| 角色 | 首页 / 壳层 | 主要菜单组 | 典型页面 | 数据范围 | 说明 |
|------|-------------|------------|----------|----------|------|
| 大仓管理员 | 普通后台壳层，主仓运营视角 | 主仓业务组 + 研发协同入口 | 主仓入出库、主仓验收、研发协同查询 | 主仓全量 + 与 RD 协同有关数据 | 面向主仓职责，不承接 RD 日常操作台 |
| 小仓管理员 | `rd-subwarehouse` 专属壳层，默认 `研发工作台` | `研发小仓` | `小仓库存`、`库存流水`、`自动入库结果`、`项目领用`、`本仓报废`、后续 `盘点/调整` | 固定本小仓范围 | 只保留极少数公共能力，不暴露主仓运营面 |
| 系统管理员 | 同时保留普通后台壳层与 RD 相关入口 | 主仓业务组 + `研发小仓` + `系统管理` | 两侧页面都可进入 | 主仓侧 + 小仓侧 + 系统配置 | 用于配置、排障、跨域核对，不应被 `consoleMode` 误裁 |

### 架构职责矩阵

| 层级 | 负责内容 | 推荐承载 | 关键约束 |
|------|----------|----------|----------|
| 前端壳层 | 菜单分组、首页入口、标签页与角色视角切换 | `web` 路由、菜单元数据、`consoleMode` | 只做视角裁切，不改变后端真实权限；`admin` 保留双视角 |
| RBAC | 菜单、页面、按钮、数据范围授权 | `rbac` | 前后端口径一致；不能出现前端多裁或后端漏兜底 |
| RD 业务编排 | 研发采购需求、小仓领用、报废、盘点 / 调整、自动入库结果 | RD 子域切片 + 现有业务模块复用 | 小仓只承接本仓内部动作与主仓协同，不承接公司级外部入出 |
| 主仓协同编排 | 主仓验收、主仓到小仓调拨 / 交接、退回 / 对外退货 | 主仓相关业务模块 | 主仓仍是公司级入出统一入口 |
| 库存核心 | 库存增减、流水、责任仓别记账 | `inventory-core` | 所有库存写操作统一入核，禁止旁路改数 |
| 查询 / 报表 | 小仓报表、主仓报表、协同链路可视化 | `reporting`、read models | 按仓别、角色隔离可见范围，避免跨仓串数 |

## 待决策项

当前无待决策项。本工作流已归档；后续 RD 新切片应另开新的 requirement / task / workspace。

## 背景与上下文

- 项目级需求已同步收录“主仓 + 研发小仓受限协同”能力，后续规划与实现应以 `PROJECT_REQUIREMENTS.md` 和新的切片 requirement 共同作为真源。
- 现有项目已经包含 `project`、`workshop-material`、`inventory-core`、`rbac` 等相关模块方向；其中 `inventory-core` 已是库存唯一写入口，`project` 已承接项目归集语义，`rbac` 已有菜单/按钮/数据权限基础。
- 基于已确认口径，这次诉求更像“主仓与研发小仓之间的受限协同”，而不是完整开放式多仓对外运营。
- 这份文档保留临时脑暴、方案对比与决策收敛过程；正式口径以归档 requirement 与 `PROJECT_REQUIREMENTS.md` 为准。

## 初步前提

1. 研发小仓是一个真实独立运转的小仓，不是单纯的研发领用标签。
2. 货物进入公司、离开公司仍统一经过主仓；研发小仓第一阶段只与主仓对接。
3. 主仓到小仓的流转应由主仓单据自动过账到小仓，不增加小仓二次确认负担。
4. 小仓的出库、盘点、报废都属于小仓内部动作，但出库必须绑定研发项目或项目式归集项。
5. 小仓管理员需要独立工作台，但只保留极少数不暴露主仓运营面的公共能力。
6. 研发采购需求由小仓侧提出、采购角色执行、主仓按采购信息验收入库，采购与验收之间应建立可选取、可追溯的单据关联。
7. 研发采购到货后应先入主仓，再按真实交接时点转入小仓；若存在时间差，不应在主仓验收时直接记入小仓。
8. 研发采购链路中的物料需要维护独立状态流，至少覆盖 `待采购 -> 采购中 -> 到货 -> 验收 -> 领取/报废/退回`，并允许在前置阶段进入 `取消`。
9. 所有库存结果仍必须经过 `inventory-core`，不能绕过库存核心直接改数。

## 研发物料状态口径（已确认）

- 这组状态描述的是“研发采购链路里的物料流转状态”，不等同于采购单、验收单、领料单自己的审核状态。
- `待采购`：小仓管理员已填写并保存采购表单，等待采购角色确认接单。
- `采购中`：采购角色已确认接单，采购执行中（或待供应商/到货）。
- `取消`：采购被驳回，或因业务原因终止，不再继续后续流转。
- `到货`：实物已到主仓，主仓可据此对照采购信息核货。
- `验收`：主仓已完成验收，并在合适时点将物料交接/转入小仓。
- `领取`：物料已在小仓侧被项目或项目式归集项领用。
- `报废`：物料在主仓或小仓链路中被判定报废，结束流转。
- `退回`：物料走公司级退回处理，仍由主仓统一承接，不要求小仓管理员直接操作。

## 方案候选

### 方案 A：受限子仓模型（推荐）

- 摘要：在同一平台内引入“主仓 + 研发小仓”仓别语义，但第一阶段只开放主仓到小仓的内部调拨，以及小仓自己的出库、盘点、报废、报表。
- 补充：同时引入“研发采购需求 -> 采购执行 -> 主仓按采购信息验收”的协同链路，采购信息服务于主仓验收自动带出。
- 补充：研发采购到货后的推荐库存路径为“主仓验收入主仓 -> 按实际交接再转小仓”，避免因时间差造成库存责任失真。
- 代价：中
- 风险：低
- 优点：
  - 最贴合当前已确认业务口径：小仓独立运转，但不承担公司级外部入出。
  - 小仓管理员工作台边界清晰，权限与报表容易按仓别裁剪。
  - 能复用 `inventory-core`、`project`、`rbac`、`reporting` 现有方向。
- 缺点：
  - 需要给库存、单据、报表增加仓别维度。
  - 需要引入主仓到小仓的专门协同单据或明确调拨语义。
- 复用点：`inventory-core`、`project`、`rbac`、`reporting`

### 方案 B：项目库存池增强模型

- 摘要：不真正引入小仓仓别，而是把研发小仓视为“项目归集库存池 + 权限视图”，主仓领料后直接转成项目库存，再由小仓页面操作。
- 代价：低
- 风险：高
- 优点：
  - 改动可能更小，更多复用现有 `project` 语义。
  - 第一版开发速度可能更快。
- 缺点：
  - 很难自然表达“小仓像独立小仓一样运转”的现实。
  - 盘点、报废、库存流水、报表容易和真实仓库语义拧巴。
  - 后续若继续扩展别的小仓，模型会越来越别扭。
- 复用点：`project`、`reporting`、`rbac`

### 方案 C：通用多仓框架

- 摘要：直接把系统升级成通用多仓架构，主仓、研发小仓以及未来其它仓位都放入统一仓库体系，配套更完整的仓间调拨、仓别报表和权限模型。
- 代价：高
- 风险：中
- 优点：
  - 长期最完整，未来扩展更多仓别最自然。
  - 模型统一，后续能力上限高。
- 缺点：
  - 超出当前“主仓 + 一个研发小仓”的最小闭环。
  - 会把第一阶段范围、数据迁移、前后端改动面都显著放大。
- 复用点：`inventory-core`、`rbac`，但需要更大范围扩展

## 最终确认方案

最终确认选择 **方案 A：受限子仓模型**。

原因：它既承认“研发小仓已经真实独立运转”，又守住“公司级入出统一经过主仓”的管理原则；相比直接上通用多仓，范围更稳；相比仅做项目库存池，又更符合你当前的真实业务。

## 关键里程碑

| 时间 | 事件 |
|------|------|
| 2026-03-25 | 新建临时 workspace 文档，开始澄清研发小仓需求 |
| 2026-03-25 | 确认研发小仓现实中已像独立小仓运转，而非单纯领用后保管 |
| 2026-03-25 | 确认小仓第一阶段仅与主仓对接；主仓领料后自动入库到小仓 |
| 2026-03-25 | 明确小仓管理员聚焦小仓内部收发盘查；公司级入出仍统一由主仓承担 |
| 2026-03-25 | 确认仓间交接自动过账到小仓，不要求小仓二次确认 |
| 2026-03-25 | 确认小仓出库必须绑定研发项目或项目式归集项 |
| 2026-03-25 | 确认隔离模式采用折中方案：小仓专用工作台 + 少量公共能力保留 |
| 2026-03-25 | 建议公共能力仅保留个人中心、物料只读选择、项目/归集项只读选择 |
| 2026-03-25 | 确认小仓可直接执行本仓报废 |
| 2026-03-25 | 输出三种建模方案，并推荐“受限子仓模型”作为正式需求起点 |
| 2026-03-26 | 已生成 `req-20260326-0048-rd-subwarehouse.md` 需求草案，并完成正式口径确认 |
| 2026-03-26 | 已将“主仓 + 研发小仓受限协同”同步为 `PROJECT_REQUIREMENTS.md` 项目级长期需求 |
| 2026-03-26 | 补充研发采购链路：小仓提采购需求、采购角色执行、主仓按采购信息验收并自动带入单据 |
| 2026-03-26 | 确认研发采购到货后先入主仓，再按真实时点转入小仓，不直接在验收时记入小仓 |
| 2026-03-26 | 补充研发物料状态流：待采购、采购中、取消、到货、验收、领取、报废、退回 |
| 2026-03-26 | 细化前置态：保存采购表单默认待采购，采购角色确认后切换采购中 |
| 2026-03-26 | 已完成 `Slice 1`：研发独立工作台、固定仓别隔离、项目领用、本仓报废、研发侧库存/流水/自动入库结果与分类报表入口，并通过浏览器冒烟验证 |
| 2026-03-28 | 将本工作流迁入 `archive/retained-completed/rd-subwarehouse/README.md`，作为 RD Phase 1 基线与决策溯源 |

## 本文件夹资产索引

| 文件 | 用途 |
|------|------|
| `README.md` | 本工作流归档说明、方案收敛与 Phase 1 基线摘要 |
